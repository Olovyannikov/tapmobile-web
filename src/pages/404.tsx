import { Layout } from '@/layouts/Layout';
import { HeroSection } from '@/components';

const ErrorPage = () => {
    return (
        <Layout>
            <HeroSection title='404 Не найдено' />
        </Layout>
    );
};

export default ErrorPage;
